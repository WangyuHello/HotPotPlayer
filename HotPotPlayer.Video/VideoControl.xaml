<base:UserControlBase
    x:Class="HotPotPlayer.Video.VideoControl"
    xmlns:base="using:HotPotPlayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotPotPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:convs="using:HotPotPlayer.Converters"
    xmlns:animations="using:CommunityToolkit.WinUI.UI.Animations"
    mc:Ignorable="d">

    <base:UserControlBase.Resources>
        <convs:SliderThumbConverter x:Key="SliderThumbConverter" TotalTime="{x:Bind CurrentPlayingDuration, Mode=OneWay}"/>
        <convs:SliderVolumeIntConverter x:Key="SliderVolumeConverter" />
        <convs:ColorConverter x:Key="ColorConverter" />
        <AcrylicBrush x:Key="PlayBarBackground" TintOpacity="0.8" TintLuminosityOpacity="0.3" TintColor="Transparent" FallbackColor="Transparent" />

        <Style x:Key="VolumePopupStyle" TargetType="FlyoutPresenter">
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="MaxWidth" Value="160" />
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="MaxHeight" Value="40" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollMode" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        </Style>

    </base:UserControlBase.Resources>

    <Grid PointerEntered="Grid_PointerEntered" PointerMoved="Grid_PointerMoved">
        <SwapChainPanel x:Name="Host" Loaded="Host_Loaded" CompositionScaleChanged="Host_CompositionScaleChanged" SizeChanged="Host_SizeChanged" Unloaded="Host_Unloaded" PointerReleased="Host_PointerReleased"/>
        
        <!--PlayBar-->
        <Grid Background="#e0424047" CornerRadius="8" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" MaxWidth="840" Margin="16,0,16,16" Padding="8,6" Visibility="{x:Bind PlayBarVisible, Mode=OneWay}" PointerEntered="PlayBar_PointerEntered" PointerExited="PlayBar_PointerExited">

            <animations:Implicit.ShowAnimations>
                <animations:TranslationAnimation Duration="0:0:1" From="0,100,0" To="0,0,0" EasingMode="EaseOut"/>
            </animations:Implicit.ShowAnimations>
            <animations:Implicit.HideAnimations>
                <animations:TranslationAnimation Duration="0:0:0.8" From="0,0,0" To="0,100,0" />
            </animations:Implicit.HideAnimations>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Margin="0,0,8,0" Orientation="Horizontal" Spacing="4">
                <Button Click="PlayButtonClick" Style="{StaticResource VideoPlayOtherButton}">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind GetPlayButtonIcon(IsPlaying, x:False), Mode=OneWay}" />
                </Button>
                <Button Style="{StaticResource VideoPlayOtherButton}" Click="PlayNextButtonClick">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xF8AD;" />
                </Button>
                <Button Style="{StaticResource VideoPlayOtherButton}" Click="PlayModeButtonClick">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind GetPlayModeIcon(PlayMode), Mode=OneWay}" />
                </Button>
            </StackPanel>

            <Slider x:Name="PlaySlider" Margin="8,0" Grid.Column="1" Translation="0,2,0" Style="{StaticResource VideoPlaySliderStyle}" Value="{x:Bind GetSliderValue(CurrentTime, CurrentPlayingDuration), Mode=OneWay}" ThumbToolTipValueConverter="{StaticResource SliderThumbConverter}" >
            </Slider>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                <TextBlock Width="35" VerticalAlignment="Center" Text="{x:Bind CurrentTime.ToString('mm\\\\:ss'), Mode=OneWay}" FontFamily="{ThemeResource MiSansRegular}" FontSize="13" Foreground="#b2b2b2"/>
                <TextBlock Margin="0,0,2,0" Text="/" VerticalAlignment="Center" FontFamily="{ThemeResource MiSansRegular}" FontSize="13" Foreground="#b2b2b2"/>
                <TextBlock Width="36" VerticalAlignment="Center" Text="{x:Bind GetDuration(CurrentPlayingDuration), Mode=OneWay}" FontFamily="{ThemeResource MiSansRegular}" FontSize="13" Foreground="#b2b2b2"/>
            </StackPanel>

            <StackPanel Margin="16,0,0,0" Grid.Column="3" Orientation="Horizontal" Spacing="8">
                <Button Style="{StaticResource VolumeButton}">
                    <local:VolumeIntPresenter Volume="{x:Bind Volume, Mode=OneWay}"/>
                    <Button.Flyout>
                        <Flyout FlyoutPresenterStyle="{StaticResource VolumePopupStyle}">
                            <Slider Margin="0,2,0,0" Value="{x:Bind Volume, Mode=TwoWay, Converter={StaticResource SliderVolumeConverter}}" IsThumbToolTipEnabled="False"/>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Button Style="{StaticResource VideoPlayOtherButton}" Click="ToggleStatusClick">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE946;" />
                </Button>
                <Button Style="{StaticResource VideoPlayOtherButton}" Click="ToggleFullScreenClick">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind GetFullScreenIcon(IsFullScreen), Mode=OneWay}" />
                </Button>
                <Button Style="{StaticResource VideoPlayOtherButton}" Click="{x:Bind MusicPlayer.TogglePlayListBarVisibility}">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE762;" />
                </Button>
            </StackPanel>
        </Grid>
        
        <!--Title-->
        <Grid VerticalAlignment="Top" Padding="8,8" Background="#e0424047" Visibility="{x:Bind PlayBarVisible, Mode=OneWay}" PointerEntered="PlayBar_PointerEntered" PointerExited="PlayBar_PointerExited">
            <animations:Implicit.ShowAnimations>
                <animations:TranslationAnimation Duration="0:0:1" From="0,-50,0" To="0,0,0" EasingMode="EaseOut"/>
            </animations:Implicit.ShowAnimations>
            <animations:Implicit.HideAnimations>
                <animations:TranslationAnimation Duration="0:0:0.8" From="0,0,0" To="0,-50,0" />
            </animations:Implicit.HideAnimations>


            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Style="{StaticResource VideoPlayOtherButton}" Click="NavigateBackClick">
                <SymbolIcon Symbol="Back" />
            </Button>
            <TextBlock Margin="8,0" Grid.Column="1" Foreground="White" VerticalAlignment="Center" Text="{x:Bind Title, Mode=OneWay}" FontFamily="{ThemeResource MiSansRegular}" FontSize="14"/>
        </Grid>
    </Grid>
    

</base:UserControlBase>
