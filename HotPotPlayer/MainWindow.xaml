<Window
    x:Class="HotPotPlayer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotPotPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <Grid>
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Background="{StaticResource MainNavBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="60" />
                </Grid.RowDefinitions>
                <Border x:Name="ButtonPop" Width="36" Height="36" CornerRadius="4" HorizontalAlignment="Center" Background="{StaticResource MainNavSelectedBackground}" Visibility="{x:Bind GetPopVisibility(SelectedPageName), Mode=OneWay}"/>
                <Button Style="{StaticResource MainNavBackButton}" Click="BackClick" IsEnabled="{x:Bind MainFrame.CanGoBack, Mode=OneWay}">
                    <SymbolIcon Symbol="Back" />
                </Button>
                <StackPanel x:Name="ButtonStack" Grid.Row="1" Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center" >
                    <Button x:Name="Music" Style="{StaticResource MainNavButton}" Click="NavigateClick" IsEnabled="{x:Bind GetEnableState('Music', SelectedPageName), Mode=OneWay}">
                        <SymbolIcon Symbol="MusicInfo" />
                    </Button>
                    <Button x:Name="Video" Style="{StaticResource MainNavButton}" Click="NavigateClick" IsEnabled="{x:Bind GetEnableState('Video', SelectedPageName), Mode=OneWay}">
                        <SymbolIcon Symbol="PreviewLink" />
                    </Button>
                    <Button x:Name="Bilibili" Style="{StaticResource MainNavButton}" Click="NavigateClick" IsEnabled="{x:Bind GetEnableState('Bilibili', SelectedPageName), Mode=OneWay}">
                        <SymbolIcon Symbol="World" />
                    </Button>
                    <Button x:Name="CloudMusic" Style="{StaticResource MainNavButton}" Click="NavigateClick" IsEnabled="{x:Bind GetEnableState('CloudMusic', SelectedPageName), Mode=OneWay}">
                        <SymbolIcon Symbol="Zoom" />
                    </Button>
                </StackPanel>
                <Button x:Name="Setting" Grid.Row="2" Style="{StaticResource MainNavButton}" Click="NavigateClick" IsEnabled="{x:Bind GetEnableState('Setting', SelectedPageName), Mode=OneWay}">
                    <SymbolIcon Symbol="Setting" />
                </Button>
            </Grid>
            <Frame
                Margin="0,28,0,0"
                Grid.Column="1" 
                x:FieldModifier="public"
            x:Name="MainFrame" />
        </Grid>
        <Grid x:FieldModifier="public" x:Name="CustomTitleBar" 
              VerticalAlignment="Top"
              Margin="60,0,0,0"
              Height="28" >

        </Grid>
    </Grid>
</Window>
