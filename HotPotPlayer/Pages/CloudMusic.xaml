<Page
    x:Class="HotPotPlayer.Pages.CloudMusic"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotPotPlayer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cont2="using:HotPotPlayer.UI.Controls"
    xmlns:helper="using:HotPotPlayer.Pages.Helper"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="White"
    >

    <Grid Background="White">
        <Pivot 
            Margin="36,28,0,0" 
            x:Name="RootPivot" 
            HeaderTemplate="{StaticResource GeneralPivotHeaderTemplate}"
            >
            <PivotItem Header="每日推荐" Margin="0">
                <GridView 
                    x:Name="RecommedListGridView" 
                    Style="{StaticResource CardGridViewStyle}"
                    ItemsSource="{x:Bind CloudMusicService.RecommedList, Mode=OneWay}" 
                    ItemClick="{x:Bind helper:ListHelper.PlayMusicInList}"
                    >
                    <GridView.Footer>
                        <Grid Height="100"/>
                    </GridView.Footer>
                </GridView>
            </PivotItem>
            <PivotItem Header="我喜欢的音乐" Margin="0">
                <GridView 
                    x:Name="LikeListGridView" 
                    Style="{StaticResource CardGridViewStyle}"
                    ItemsSource="{x:Bind CloudMusicService.LikeList, Mode=OneWay}" 
                    ItemClick="{x:Bind helper:ListHelper.PlayMusicInList}"
                    >
                    <GridView.Footer>
                        <Grid Height="100"/>
                    </GridView.Footer>
                </GridView>
            </PivotItem>
        </Pivot>

        <StackPanel 
            Margin="0,6,160,0" 
            Orientation="Horizontal" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Right" 
            Spacing="8"
            >
            <cont2:ImageEx2 
                Source="{x:Bind CloudMusicService.Avatar, Mode=OneWay}" 
                Style="{StaticResource LocalMusicImageStyle}" 
                Width="32" Height="32"
                CornerRadius="16"
                />
            <TextBlock 
                VerticalAlignment="Center" 
                Text="{x:Bind CloudMusicService.NickName, Mode=OneWay}" 
                FontFamily="{ThemeResource MiSansRegular}"
                />
        </StackPanel>
    </Grid>
</Page>
