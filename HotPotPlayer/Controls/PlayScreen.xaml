<UserControl
    x:Class="HotPotPlayer.Controls.PlayScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotPotPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:me="using:HotPotPlayer.Media"
    xmlns:cont="using:HotPotPlayer.UI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="Root" Background="LightGray" x:Load="{x:Bind MusicPlayer.IsPlayScreenVisible, Mode=OneWay}">
        <Button VerticalAlignment="Top" HorizontalAlignment="Left" Margin="52,40" Click="{x:Bind MusicPlayer.ToggleShowPlayScreen}">
            <SymbolIcon Symbol="Clear" />
        </Button>

        <cont:ImageEx2 x:Name="Cover" Margin="52,100,0,0"
            Style="{StaticResource PlayScreenImageStyle}" 
            Source="{x:Bind MusicPlayer.CurrentPlaying.Cover, Mode=OneWay}" 
            />

    </Grid>
</UserControl>
